# SCRUM-133 대피 동선 통합·3D 작업 인계

기준일: 2026-08-25

브랜치: `feat/SCRUM-133`

현재 완료 지점: **5/19**
마지막 기능 커밋: `d02071da feat(SCRUM-133): 도면 대피 경로를 단일 엔진 호출로 계산`

이 문서는 대피 동선 엔진 단일화와 도면 3D 보기까지 남은 19개 작업의 진행 상태를 관리하는 현행 인계 문서다. 작업자는 아래 작업을 시작하거나 끝낼 때마다 반드시 이 문서를 함께 갱신한다.

## 갱신 규칙

각 작업을 수행할 때 다음 순서를 지킨다.

1. 시작 전에 해당 행의 상태를 `진행 중`으로 바꾸고 시작일, 기준 커밋과 구현 범위를 작업 기록에 남긴다.
2. 계획과 다른 계약 변경, 우회 구현 또는 새 위험이 생기면 발견 즉시 작업 기록과 결정 사항에 적는다.
3. 구현 후 해당 영역의 필수 검증을 직접 실행한다. 실행하지 못한 검증은 통과로 표시하지 않는다.
4. 기능과 직접 테스트를 최소 작업 단위의 원자 커밋으로 남긴다. 저장소의 `feat(SCRUM-133): ...` 메시지 관례를 따른다.
5. 완료 후 상태를 `완료`로 바꾸고 완료일, 커밋 SHA, 주요 파일, 검증 결과와 남은 위험을 기록한다.
6. 문서 갱신은 해당 작업 커밋에 포함하거나 바로 다음 문서 전용 커밋으로 남긴다. 다음 작업을 먼저 시작하지 않는다.
7. 순서를 바꾸거나 작업을 보류·취소하면 원래 행을 삭제하지 말고 상태와 이유를 남긴다.

상태 값은 `대기`, `진행 중`, `완료`, `보류`, `취소`만 사용한다. 검증 결과가 확인되지 않은 작업은 `완료`로 표시하지 않는다.

## 전체 진행표

| 번호 | 작업 | 난이도 예상 | 상태 | 완료 커밋 |
| ---: | --- | --- | --- | --- |
| 1 | 엔진 커버리지 필드 직렬화 | 보통낮음 | 완료 | `2f58c08b` |
| 2 | Java 엔진 응답 계약 수용 | 보통낮음 | 완료 | `f9b37793` |
| 3 | 구역 경계 트리밍 순수 로직 | 보통 | 완료 | `5bd3be6d` |
| 4 | 커버리지 기반 구역별 비상구 분기 추출 | 보통 | 완료 | `682bf16a` |
| 5 | 도면 전체를 단일 엔진 호출로 전환 | 보통 | 완료 | `d02071da` |
| 6 | 응답 계약에서 타일 제거 | 보통낮음 | 대기 | - |
| 7 | 레이아웃 버전 캐시 적용 | 보통낮음 | 대기 | - |
| 8 | 권한과 직원 가시성 통합 | 보통 | 대기 | - |
| 9 | 기존 Java 라우팅 삭제 | 보통 | 대기 | - |
| 10 | 정적 3D 장면 입력 계약 정의 | 보통낮음 | 대기 | - |
| 11 | 비상구와 기본 기하 생성기 추출 | 보통 | 대기 | - |
| 12 | 바닥·외곽벽 공용 장면 추출 | 보통 | 대기 | - |
| 13 | 내벽·기둥·구조물 공용화 | 보통 | 대기 | - |
| 14 | 비상구 포털과 정적 장면 수명주기 완성 | 보통 | 대기 | - |
| 15 | 도면 읽기 전용 3D 스테이지 추가 | 보통 | 대기 | - |
| 16 | 도면 2D/3D 전환 연결 | 보통낮음 | 대기 | - |
| 17 | 3D 대피 경로 레이어 | 보통 | 대기 | - |
| 18 | 3D 흐름 애니메이션 | 보통 | 대기 | - |
| 19 | 3D 최종 회귀 및 성능 검증 | 보통 | 대기 | - |

## 완료된 작업 기록

### 1. 엔진 커버리지 필드 직렬화

- 완료일: 2026-08-25
- 커밋: `2f58c08b feat(SCRUM-133): 엔진 미리보기에 비상구 커버리지를 추가`
- 결과: `GridRouter.exit_label`을 1m 래스터로 다운샘플해 `routes.json.coverage`로 내보낸다.
- 계약: `originX`, `originY`, `step`, `columns`, `rows`, `labels`, `exitIds`.
- 범위: 미리보기 직렬화만 변경했으며 실제 시뮬레이션 라우팅과 물리는 변경하지 않았다.

### 2. Java 엔진 응답 계약 수용

- 완료일: 2026-08-25
- 커밋: `f9b37793 feat(SCRUM-133): Java에 경로 커버리지 응답 계약을 추가`
- 결과: `RoutePreviewResult`와 `RouteCoverage`를 추가하고 숫자 범위, 래스터 크기와 라벨 인덱스를 역직렬화 경계에서 검증한다.
- 호환성: 기존 `previewRoutes()` 호출자는 그대로 유지했다.

### 3. 구역 경계 트리밍 순수 로직

- 완료일: 2026-08-25
- 커밋: `5bd3be6d feat(SCRUM-133): 구역 경계부터 대피 경로를 표시`
- 결과: `ZoneBoundaryTrimmer`가 경로의 최초 구역 이탈 교차점 이전을 제거한다.
- 예외: 비상구가 구역 안에 있거나 교차점이 없으면 원래 경로를 유지한다.

### 4. 커버리지 기반 구역별 비상구 분기 추출

- 완료일: 2026-08-25
- 커밋: `682bf16a feat(SCRUM-133): 커버리지에서 구역별 대피 갈래를 추출`
- 결과: `ZoneCoverageBranchExtractor`가 구역 내부 라벨을 비상구별로 집계하고 각 영역의 실제 샘플 중 무게중심에 가장 가까운 대표점을 고른다.
- 판정: 미지정 샘플 수와 전체 미지정인 고립 구역을 구분한다.

### 5. 도면 전체를 단일 엔진 호출로 전환

- 완료일: 2026-08-25
- 커밋: `d02071da feat(SCRUM-133): 도면 대피 경로를 단일 엔진 호출로 계산`
- 결과: `EvacuationPreviewService.previewAll()`이 도면당 엔진 프로세스를 한 번만 실행하고 구역별 갈래를 재조립한다.
- 계약 보완: 대표점은 커버리지 결과가 나온 뒤에야 알 수 있으므로 `routePreviewZones` 요청과 `zoneRoutes` 응답을 추가했다. 엔진이 커버리지를 만든 직후 같은 프로세스 안에서 대표점을 계산하고 라우팅한다.
- 처리 범위:
  - 기본 비상구가 지정된 구역은 해당 출구를 강제한다.
  - 다른 컴포넌트가 고립되어도 도달 가능한 구역 결과는 유지한다.
  - 갈래 경로에 `ZoneBoundaryTrimmer`를 적용한다.
  - 엔진 실행 장애와 실제 도달 불가를 구분한다.
- 검증:
  - Python 엔진 unittest 171개 통과, 9개 스킵.
  - `simulation-service` `spotlessCheck` 통과.
  - `simulation-service` 전체 `test` 통과.

## 다음 작업 상세

### 6. 응답 계약에서 타일 제거

- `ZoneExitPartitionDto.tiles`를 제거하고 `entryPoint`를 추가한다.
- `entryPoint`는 경계 트리밍 후 `waypoints[0]`과 같아야 한다.
- 프론트 `RouteTile`과 타일 채색 렌더링을 제거한다.
- `ZoneExitPartition` 타입과 관련 테스트 fixture를 새 계약으로 바꾼다.
- 독립 대피 화면과 도면 편집기 오버레이 모두 갈래 경로가 계속 보이는지 확인한다.
- 주의: 5번 구현은 현 DTO와의 컴파일 호환을 위해 `tiles`에 빈 배열을 넣는다. 6번이 끝나기 전까지 이 과도 상태를 정상 최종 계약으로 간주하지 않는다.

### 7. 레이아웃 버전 캐시 적용

- `layoutVersionId` 단위로 전체 대피 경로 결과를 캐시한다.
- 같은 버전의 두 번째 호출이 엔진을 실행하지 않는지 검증한다.
- 새 도면 버전은 별도 계산하며 실패 결과는 캐시하지 않는다.
- 캐시 크기, 수명과 무효화 책임을 작업 기록에 명시한다.

### 8. 권한과 직원 가시성 통합

- `previewAll`의 안전 담당자 전용 차단을 구역 가시성 필터로 바꾼다.
- `LayoutMetadataService.readMetadata`가 사용하는 기존 가시성 규칙을 재사용한다.
- 안전 담당자는 전체 구역, 일반 직원은 자신에게 배정된 구역만 받아야 한다.
- 생산 코드와 권한 테스트에서 서비스 경계를 새로 만들지 않는다.

### 9. 기존 Java 라우팅 삭제

- 엔진 기반 경로가 충분히 검증된 뒤에만 수행한다.
- 삭제 대상: `EvacuationGrid`, `EvacuationRoutePlanner`, `EvacuationExitField`와 더 이상 유효하지 않은 대응 테스트.
- `rg`로 모든 참조가 사라졌는지 확인한다.
- `simulation-service` 전체 테스트와 실제 다구역 도면의 호출 시간 및 결과를 확인한다.

### 10. 정적 3D 장면 입력 계약 정의

- 바닥, 외곽벽, 내벽, 기둥, 구조물과 비상구를 표현하는 정적 입력 타입을 `SimulationResultViewModel`에서 분리한다.
- 좌표 변환, 높이, 색상과 재질 경계를 정한다.
- 아직 기존 렌더러 구현을 이동하지 않는다.

### 11. 비상구와 기본 기하 생성기 추출

- `createBoundarySegments`, `exitPortalPlacement`, `worldToScene` 등 기존 순수 기하 헬퍼를 공용 위치로 옮긴다.
- 기존 export와 테스트를 새 위치로 함께 이전한다.
- 시뮬레이션 결과 화면의 3D 회귀를 확인한다.

### 12. 바닥·외곽벽 공용 장면 추출

- `floorPlanScene`에 바닥과 외곽벽 생성을 옮긴다.
- 기존 시뮬레이션 3D가 공용 장면을 사용하도록 연결한다.
- 변경 전후 실제 화면 캡처를 비교한다.

### 13. 내벽·기둥·구조물 공용화

- 나머지 정적 구조물을 공용 장면 생성기로 옮긴다.
- 기존 재질, 높이, 그림자와 좌표를 보존한다.
- 단위 테스트와 실제 시뮬레이션 3D를 함께 확인한다.

### 14. 비상구 포털과 정적 장면 수명주기 완성

- 비상구 포털 렌더링을 공용 장면으로 옮긴다.
- 장면 생성과 해제 책임을 정리하고 geometry와 material의 dispose를 검증한다.
- 기존 시뮬레이션 3D 전체 기능을 브라우저에서 재확인한다.

### 15. 도면 읽기 전용 3D 스테이지 추가

- 공용 정적 장면을 사용하는 `LayoutThreeStage`를 추가한다.
- 카메라, OrbitControls, resize와 정리 처리를 구현한다.
- 3D에서는 편집 입력을 비활성화하고 빈 데이터와 WebGL 오류 상태를 처리한다.

### 16. 도면 2D/3D 전환 연결

- 기존 `SimulationViewToggle` 패턴을 재사용한다.
- 대피 동선이 켜진 상태에서 3D를 선택할 수 있게 한다.
- 읽기 전용임을 알리고 2D 편집 상태를 보존한다.

### 17. 3D 대피 경로 레이어

- 직각 폴리라인을 바닥 위 리본 또는 얇은 튜브로 표현한다.
- 비상구별 색과 여러 구역·갈래를 지원한다.
- 카메라 거리와 각도에 따른 가독성을 브라우저에서 확인한다.

### 18. 3D 흐름 애니메이션

- 경로 텍스처의 UV 또는 오프셋을 이동해 2D와 같은 방향감을 준다.
- `prefers-reduced-motion`에서는 정지한다.
- 프레임 저하, 재진입과 리소스 해제를 확인한다.

### 19. 3D 최종 회귀 및 성능 검증

- 도면 3D의 토글, 카메라, 다중 갈래와 흐름을 확인한다.
- 기존 시뮬레이션 결과 3D의 에이전트, 재생, 히트맵과 병목을 확인한다.
- resize, 재진입과 2D/3D 반복 전환을 검증한다.
- 콘솔 오류, 메모리 누수와 성능 결과를 기록한다.

## 현재 남아 있는 위험과 경계

- 6번 전까지 `ZoneExitPartitionDto.tiles`는 계약에 남아 있지만 5번 엔진 기반 분기는 빈 배열을 반환한다.
- 기존 Java 라우팅 클래스와 테스트는 9번까지 저장소에 남는다. `previewAll`은 더 이상 이를 사용하지 않지만 직원 단건 경로와 남은 참조를 삭제 전에 다시 확인해야 한다.
- 5번의 단일 호출은 Java 메서드 호출 횟수만 줄인 것이 아니라 Python 프로세스 내부에서 커버리지와 대표 경로를 함께 계산한다. 이를 다시 두 프로세스 호출로 분리하지 않는다.
- `routePreviewZones` 모드에서는 일부 연결 컴포넌트가 고립되어도 도달 가능한 컴포넌트를 반환한다. 일반 시뮬레이션과 기존 단건 미리보기의 오류 계약은 완화하지 않는다.
- 3D 공용화는 기존 669줄 시뮬레이션 렌더러의 회귀 위험이 크다. 11~14번은 기계적 추출과 실제 브라우저 회귀 확인을 함께 수행한다.

## 관련 완료 작업

아래 작업은 19개 진행표 밖에서 이미 완료되었으며 되돌리지 않는다.

- `542d8c49`: 미리보기 대피 경로 직각 정형화.
- `3ea10148`: 도면 편집기에 구역별 대피 동선 토글과 흐름 오버레이 통합.
- 시뮬레이션 실제 이동 경로는 대각선 이동을 유지하며 직각화는 표시용 미리보기에만 적용한다.

## 검증 명령

```powershell
cd apps/simulation-service
Push-Location engine
.\.venv\Scripts\python.exe -m unittest discover -s . -p "test_*.py"
Pop-Location
.\gradlew.bat spotlessCheck
.\gradlew.bat test

cd ..\..
pnpm --filter @hwalro/frontend lint
pnpm --filter @hwalro/frontend build
pnpm --filter @hwalro/frontend test
```

프론트 UI를 변경한 작업은 빌드·테스트만으로 완료하지 않는다. 실제 브라우저에서 관련 페이지와 상호작용을 확인하고 캡처 또는 명확한 관찰 결과를 작업 기록에 남긴다. 3D 작업은 도면 화면뿐 아니라 기존 시뮬레이션 결과 3D도 반드시 함께 확인한다.
